<UserControl x:Class="NewCal.Views.AboutView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#ffffff" Offset="0"/>
                <GradientStop Color="#f3f7ff" Offset="1"/> 
            </LinearGradientBrush>
        </Grid.Background> 
         
        <!-- 中央卡片 -->
        <Border Width="720" Height="380" CornerRadius="12" Background="White" 
                HorizontalAlignment="Center" VerticalAlignment="Center" Padding="18"
                BorderBrush="#E8EEF8" BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="作者 小猿本尊" FontSize="48" FontFamily="STZhongsong" HorizontalAlignment="Center" />
                    <TextBlock Text="版本 1.0.0" FontSize="14" Foreground="#666" Margin="0,8,0,0" HorizontalAlignment="Center"/>
                </StackPanel>

                <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,12,0,0">
                    <Button Content="返回上一页" Click="BackWithConfetti_Click" Width="220" Height="56" Margin="8"/>
                    <Button Content="庆祝一下" Click="Celebration_Click" Width="140" Height="56" Margin="8"/>
                </StackPanel>
            </Grid>
        </Border>
        <!-- Confetti Canvas: 在最上层，所有彩带绘制在这里 -->
        <Canvas x:Name="ConfettiCanvas" IsHitTestVisible="False" />

    </Grid>
</UserControl>
